#ifndef _mcac_H_
#define _mcac_H_
// #include "ppeInit.h"

namespace bm
{
    extern int is_in(char *s, char c);
    extern int isOpsymbol(char ch);
    extern int level(char op);
    extern int calcValue(int od1, int od2, char tempop);
    // extern int calcDirect(char str[], lb_list *L);
    extern int is_in(char *s, char c);
    extern int is_in(char *s, char c);
    extern int levelOp(char op);

}

#endif